方案建议做成一个“管理型 RPC 清空”流程，不靠本地脚本删目录。

  1. 在 startClients.sh 里新增控制台命令，比如 reset 或 clear

  - 这个命令不带文件路径。
  - 它会发起一次专门的管理请求到 manager。

  2. 给 manager 增加一个管理 RPC，例如 ResetSystem

  - manager 收到后先进入短暂维护态，暂停接收新的业务写请求，避
    免清空过程中产生新数据。
  - 然后逐个调用所有 storager 的清空接口。
  - 所有 storager 返回成功后，manager 自己清空路由缓存、root
    hash、root accumulator、统计信息等状态。
  - 最后退出维护态。

  3. 给每个 storager 增加一个清空 RPC，例如 ResetStorage

  - 清空内存中的 ADS 数据。
  - 如果是带持久化的实现，还要清空对应磁盘状态目录，但进程不退
    出。
  - 返回清空结果。

  4. 处理并发和一致性

  - 清空前先让 manager 等待正在处理的请求完成，或先拒绝新请求，
    再清空。
  - 清空完成后再恢复在线。
  5. 目录/进程约束

  - 不假定 clients、manager、storagers 在同一台机器。
  - 所以不能用 scripts/stopSNs.sh 这种本地文件式方案。